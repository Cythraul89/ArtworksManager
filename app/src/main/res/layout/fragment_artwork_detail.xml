<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/primary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:navigationIcon="@drawable/ic_back"
        app:titleTextColor="@color/on_primary" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/photo"
                android:layout_width="match_parent"
                android:layout_height="280dp"
                android:scaleType="centerCrop"
                android:background="@color/divider"
                android:contentDescription="@null"
                android:visibility="gone" />

            <!-- Additional photos strip (hidden when empty) -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/additionalPhotosRecycler"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="8dp"
                android:paddingTop="8dp"
                android:paddingBottom="4dp"
                android:nestedScrollingEnabled="false"
                android:visibility="gone" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <TextView
                    android:id="@+id/title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"
                    android:layout_marginBottom="4dp" />

                <TextView
                    android:id="@+id/artistYear"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="16sp"
                    android:textColor="@color/text_secondary"
                    android:layout_marginBottom="16dp" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:background="@color/divider"
                    android:layout_marginBottom="16dp" />

                <!-- Type -->
                <TextView android:id="@+id/labelType" style="@style/FieldLabel" android:text="@string/type" />
                <TextView android:id="@+id/valueType" style="@style/FieldValue" />

                <!-- Medium -->
                <TextView android:id="@+id/labelMedium" style="@style/FieldLabel" android:text="@string/medium" />
                <TextView android:id="@+id/valueMedium" style="@style/FieldValue" />

                <!-- Dimensions -->
                <TextView android:id="@+id/labelDimensions" style="@style/FieldLabel" android:text="@string/dimensions" />
                <TextView android:id="@+id/valueDimensions" style="@style/FieldValue" />

                <!-- Location -->
                <TextView android:id="@+id/labelLocation" style="@style/FieldLabel" android:text="@string/location" />
                <TextView android:id="@+id/valueLocation" style="@style/FieldValue" />

                <!-- Acquired -->
                <TextView android:id="@+id/labelAcquired" style="@style/FieldLabel" android:text="@string/acquired" />
                <TextView android:id="@+id/valueAcquired" style="@style/FieldValue" />

                <!-- Description -->
                <TextView android:id="@+id/labelDescription" style="@style/FieldLabel" android:text="@string/description" />
                <TextView android:id="@+id/valueDescription" style="@style/FieldValue" />

            </LinearLayout>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</LinearLayout>
