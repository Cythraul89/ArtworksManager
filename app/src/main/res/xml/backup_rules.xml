<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude encrypted prefs file so credentials are never in ADB/cloud backups -->
    <exclude domain="sharedpref" path="nextcloud_prefs.xml" />
</full-backup-content>
